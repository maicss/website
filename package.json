{
  "scripts": {
    "dev": "vitepress dev",
    "build": "pnpm run lint && vitepress build",
    "preview": "vitepress preview",
    "lint": "zhlint pages/**/*.md --fix",
    "bp": "vitepress build && vitepress preview"
  },
  "type": "module",
  "devDependencies": {
    "vitepress": "1.0.0-rc.25",
    "zhlint": "^0.7.1"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search",
        "search-insights"
      ]
    }
  },
  "dependencies": {
    "feed": "^4.2.2",
    "giscus": "^1.3.0",
    "vue": "^3.3.8"
  }
}
